# Block all direct HTTP access to this folder. Config is read server-side
# via PHP require_once only — it should never be requestable as a URL.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
